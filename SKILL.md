---
name: feishu-simple-kb
description: "飞书三层个人知识库管理：创建和维护只包含“收件箱、笔记、归档”的飞书/Lark 知识库，支持把用户口述或文本存入收件箱、从历史笔记中语义检索、整理收件箱到笔记、把过期内容移入归档。Use when the user says 安装飞书知识库管理 skill、创建知识库、存一下、记录一下、查一下之前的笔记、整理知识库、归档笔记, or wants a low-friction personal knowledge base operated by Codex/lark-cli."
---

# Feishu Simple KB

用飞书 Wiki/Docx 建一个低摩擦个人知识库。只暴露三个用户心智节点：`收件箱`、`笔记`、`归档`。复杂的写入、检索、整理规则由 Agent 执行，不要求用户理解目录、标签或导航。

底层采用 LLM Wiki 模式：用户负责提供材料、提问和判断重点；Agent 负责把材料持续编译成可复用的知识库，而不是每次问答都从零检索和拼接。

## Core Contract

- 默认用中文与用户沟通。
- 默认使用飞书个人知识库或用户指定的 Wiki 空间；优先 `--as user`。
- 如果 `lark-cli` 已登录且具备权限，直接使用，不要求用户重复登录或重新授权；只有命令明确失败时才进入授权排查。
- 不保存、输出、复制任何 token、密码、私钥或登录凭据。
- 不创建复杂分类目录；除非用户明确要求，保持三层结构。
- 不删除用户内容。归档只移动到 `归档`；合并、覆盖、删除必须单独确认。
- 写操作前确认目标范围；用户明确说“创建知识库”“存一下”“整理一下”“归档这些”时视为已授权该项写操作。
- 如果本地没有飞书 CLI、未登录、或缺少权限，按 `references/setup-and-auth.md` 处理。

## Required Structure

创建或维护以下 Wiki 节点：

```text
个人知识库
├── 00-控制台
├── 收件箱
├── 笔记
└── 归档
```

`00-控制台` 是系统页，不向用户强调为“第四个文件夹”。它用于保存可重建的配置摘要、节点 URL、整理规则和最近一次整理时间。用户真正使用的只有 `收件箱`、`笔记`、`归档`。

控制台要承担传统知识库“总导航”的作用，但不要要求用户手动维护多层导航。用户问入口、规则、最近整理状态时，优先读控制台。

## LLM Wiki Operating Model

- `收件箱` = raw sources。保留用户原话、粘贴材料、临时记录和待处理任务；默认不改写原始事实。
- `笔记` = compiled wiki。Agent 从收件箱提炼稳定知识，维护主题页、摘要页、观点页、对比页和关联关系。
- `00-控制台` = schema + index + log。保存结构约定、索引、最近操作日志、待办视图和健康检查结果。
- `归档` = historical context。保留不再活跃但可能有用的历史记录，不作为默认查询入口。

核心原则：知识应当累积。新增内容进入知识库时，Agent 不只保存它，还要判断它是否更新了已有认知、是否补充了某个主题、是否与旧结论矛盾、是否值得沉淀为新笔记。

## Workflow Decision Tree

1. 用户要“安装 / 配置 / 登录 / 第一次使用”：读取 `references/setup-and-auth.md`。
2. 用户要“创建知识库”：执行 [Create KB](#create-kb)。
3. 用户说“存一下 / 记录一下 / 帮我记”：执行 [Capture](#capture)。
4. 用户说“查一下 / 我之前记过什么”：执行 [Search](#search)。
5. 用户说“整理 / 定期整理 / 清收件箱”：执行 [Organize](#organize)。
6. 用户说“归档 / 过期了 / 放到归档”：执行 [Archive](#archive)。
7. 用户说“体检 / 健康检查 / lint / 看看知识库有没有问题”：执行 [Lint](#lint)。
8. 用户发来链接并要求记录、保存、整理：先执行 [Link Capture](#link-capture)，再进入 [Capture](#capture)。
9. 需要具体飞书命令模式时读取 `references/command-patterns.md`。
10. 需要理解本 skill 背后的知识库组织经验时读取 `references/local-kb-patterns.md` 和 `references/llm-wiki-patterns.md`。

## Target KB Resolution

目标：用户只说“记录到飞书知识库”时，先确认写入哪个知识库，避免写错空间。

1. 如果当前对话或控制台已明确目标知识库，且用户继续围绕同一知识库操作，直接使用该目标。
2. 如果用户给出具体知识库名称、Wiki URL 或控制台 URL，按该目标执行。
3. 如果用户只说“飞书知识库 / 我的知识库 / 记录到飞书”，且没有明确目标：
   - 先确认本地 `lark-cli` 是否可用；已登录且有权限就直接列出知识库，不要求重复登录。
   - 用 `lark-cli wiki +space-list --as user --format json` 检索用户可访问的知识库空间。
   - 优先挑选名称最像个人知识库的候选，如 `个人知识库`、`知识库`、`AI全栈工程师练级` 等；如果只有一个候选，询问“是记录到 XXX 里面吗？”。
   - 如果有多个候选，列出 3-5 个名称和简短说明，请用户选择；不要猜测写入。
   - 用户确认后，读取或创建该空间下的三层结构与 `00-控制台`，再执行写入。
4. 如果 space 列表命令因未登录或权限失败，再按 `references/setup-and-auth.md` 处理。

## Create KB

目标：一次性创建可用结构，并留下可恢复的控制台。

1. 检查本地是否可调用 `lark-cli`；不可用时读取 `references/setup-and-auth.md`。
2. 选择目标位置：
   - 用户指定现有飞书知识库名称（如飞书 UI 里的某张知识库卡片）时，先通过 Wiki space 列表精确匹配名称，拿到 `space_id` 后创建到该知识库根下。
   - 用户给出知识库 URL 或某个 Wiki 节点 URL 时，先解析 URL，拿到对应 `space_id` / `node_token` 后创建到该知识库或节点下。
   - 用户没有指定目标时，默认在 `my_library` 下创建根节点 `个人知识库`。
3. 在根节点下创建 `00-控制台`、`收件箱`、`笔记`、`归档` 四个 docx 节点。
4. 写入 `00-控制台`：
   - 根节点名称和 URL
   - 三个用户节点的 URL / token 摘要
   - Schema：三层节点职责、状态字段、标签字段、合并/归档规则
   - Index：`笔记` 里的主题页清单，每条包含链接、一句话摘要、类型、最近更新
   - Log：按时间追加记录 capture / ingest / query / lint / archive
   - 写入规则：新内容先入收件箱；稳定内容入笔记；过期内容入归档
   - 常用入口：收件箱、笔记、归档、最近整理记录
   - 整理规则：不删除，只移动或追加；合并前确认
5. 给用户返回知识库入口 URL 和三个常用动作示例：
   - “存一下今天的想法：...”
   - “查一下我之前关于写作的笔记”
   - “整理一下收件箱”

## Capture

目标：让记录发生，不让用户为格式停下来。

1. 先按 [Target KB Resolution](#target-kb-resolution) 确认目标知识库；没有明确目标时先列知识库并问用户确认。
2. 如果用户提供了链接，先按 [Link Capture](#link-capture) 提取内容，再把提取结果作为原始材料写入。
3. 如果用户提供了明确标题，使用标题；否则从内容生成一个短标题。
4. 新建到 `收件箱`，标题格式：`YYYY-MM-DD HHmm - 短标题`。
5. 判断内容类型并写入轻量元数据：`工作`、`生活`、`学习`、`表达`、`系统`、`待办`、`流水账`、`素材` 之一或多个。它们是标签，不是文件夹。
6. 文档正文使用这个结构：

```markdown
# 标题

记录时间：YYYY-MM-DD HH:mm
来源：用户口述 / 用户粘贴 / 对话总结 / 链接提取
来源链接：如有 URL，写原始链接
状态：inbox
类型：工作 / 生活 / 学习 / 表达 / 系统 / 待办 / 流水账 / 素材
标签：#待整理 #自动生成标签

## 原始记录
保留用户原话或忠实整理后的内容。

## 可选提炼
- 仅当用户内容明显包含结论、行动项、观点时添加。
```

7. 如果内容显然是任务，添加 `任务状态：未完成`、`截止时间：未知/用户给定时间`、`责任人：我/用户给定对象`。
8. 更新控制台 Log 表格：先读取控制台，保留已有 Log 行，把新记录并入表格后整体 overwrite 控制台。不要用 docs append 追加 Markdown 表格行。
9. 内容明显有长期价值时，执行轻量 ingest：查找 `笔记` 中相关主题页，给出“已存入收件箱，并建议整理到 X 主题”的提示；不要自动合并。
10. 写入后只回简短结果：已存入 `收件箱`，附链接。
11. 不要为了分类追问用户；分类留到整理阶段。

## Link Capture

目标：用户发链接时，先把链接内容提取成可保存的文本，再进入知识库写入。

1. 当前只内置支持微信公众号文章链接，域名通常是 `mp.weixin.qq.com`。
2. 使用 `tools/extract_wechat_article.py`：

```bash
python3 tools/extract_wechat_article.py "<WECHAT_ARTICLE_URL>" --format markdown
```

3. 提取成功后，把输出作为 Capture 的原始材料，正文里保留：
   - 原始 URL
   - 标题
   - 作者 / 公众号名（能提取到时）
   - 发布时间（能提取到时）
   - 摘要（能提取到时）
   - 正文文本
4. 提取失败时，不要伪造文章内容。把链接本身存入收件箱，并标记 `状态：inbox`、`标签：#链接待提取`，告诉用户需要稍后重试或手动粘贴正文。
5. 以后支持其他网站时，新增独立工具或在该脚本中增加 domain handler，不把临时网页解析逻辑写进主流程。

## Search

目标：用户按自然语言问，Agent 按语义找。

1. 优先读取 `00-控制台` 获取根节点和三节点范围。
2. 查询优先级：`笔记` > `收件箱` > `归档`。用户问“最近/刚才/待办”时优先 `收件箱`；用户问“长期观点/以前总结”时优先 `笔记`；用户问“旧资料/已经结束”时包含 `归档`。
3. 先用标题、摘要、关键词缩小候选；必要时 fetch 候选文档正文。
4. 如果一次问答产生了新的稳定结论、对比表、清单或方法论，询问用户是否把这个答案沉淀成 `笔记`；用户确认后创建或更新相关主题页。
5. 回答时给出：
   - 直接答案或结论
   - 相关笔记标题和链接
   - 找不到时说明检索范围，不要假装存在
6. 如果发现多个零散记录强相关，可建议“是否合并成一篇笔记”，但不要自动合并。
7. 更新控制台 Log 表格：先读取控制台，保留已有 Log 行，把 query 记录并入表格后整体 overwrite 控制台。不要用 docs append 追加 Markdown 表格行。

## Organize

目标：把收件箱变轻，把稳定知识沉淀到 `笔记`。

1. 列出 `收件箱` 中待整理文档。
2. 对每条判断动作：
   - `move_to_notes`：内容有长期价值，移动到 `笔记`。
   - `keep_in_inbox`：信息不完整、近期还会继续补充。
   - `archive`：已过期、低价值、只需保留记录。
   - `merge_candidate`：和现有笔记高度相关，需要用户确认后合并。
3. 按通用语义分流：工作事项、生活记录、学习输入、表达素材、系统/Agent 方法、任务待办、流水账、长期观点。分流结果写成标签和摘要，不创建同名文件夹。
4. 普通移动可以执行；合并、覆盖、删除必须先给计划并等待确认。
5. 移动到 `笔记` 时，不只是移动文件：要把内容编译成稳定知识页，补充 `整理摘要`、关联主题、来源链接、更新原因。
6. 对未完成任务，优先留在 `收件箱`，并在控制台的“待办视图”里记录链接。
7. 如果新内容修正了旧笔记，要更新旧笔记并标明“本次更新来源”；如果新内容和旧结论冲突，把旧笔记状态标为 `contradicted` 或添加“待核对”段落，不要静默覆盖。
8. 整理完成后更新 `00-控制台` 的 Index、Log、最近整理时间和本次移动统计。

## Archive

目标：降低噪音，不丢数据。

1. 用户指定内容时，仅归档指定范围。
2. 用户泛泛说“归档过期内容”时，先列候选和理由。
3. 归档动作只移动到 `归档`，并更新正文 `状态：archived`、`归档原因`。
4. 不删除归档内容。

## Lint

目标：像维护代码库一样维护知识库健康。

1. 读取 `00-控制台` 的 Index 和 Log，再抽样或分页读取 `笔记`。
2. 检查：
   - `笔记` 中有没有没有摘要、没有类型、没有来源链接的页面。
   - `收件箱` 中有没有长期未整理的高价值记录。
   - 是否存在多个主题相近、应该合并或互链的笔记。
   - 是否存在新记录推翻旧结论但旧笔记未更新。
   - 是否存在重要概念反复出现但还没有独立主题页。
3. 只输出健康检查报告和建议；批量移动、合并、归档必须等用户确认。
4. 用户确认执行后，按 [Organize](#organize) 或 [Archive](#archive) 处理。
5. 更新控制台 Log 表格：先读取控制台，保留已有 Log 行，把 lint 记录并入表格后整体 overwrite 控制台。不要用 docs append 追加 Markdown 表格行。

## Naming And Writing Rules

- 标题短、可扫读，不追求文学性。
- 保留用户原话优先于过度润色。
- 标签最多 5 个，使用语义标签，不把标签当目录。
- 一篇笔记只承担一个主要主题；主题相近但来源不同的内容可先并存。
- 对公众号、复盘、长期观点这类内容，优先沉淀到 `笔记`，并保留来源记录。
- 目录是给人降低压力的，标签和控制台是给 Agent 找东西的。
- 用户没有明确要求时，不把三层结构扩展成复杂分区。
- 每篇稳定笔记应尽量包含：一句话摘要、类型、状态、来源链接、最近更新时间、相关笔记。
- 状态可用：`inbox`、`draft`、`active`、`stale`、`contradicted`、`archived`。
- 控制台 Log 语义上只追加历史记录，但实现上必须重写完整 Log 表格或使用表格结构化 API；不要用 docs append 追加 Markdown 表格行。Index 可以更新。
- 控制台优先使用表格和短链接文本（如 `[打开](URL)`），避免裸 URL 造成版面过宽。

## Failure Handling

- 找不到控制台：询问用户是否提供知识库入口 URL；如果用户同意，重新扫描根节点并重建控制台。
- 找不到某个三层节点：在根节点下补建缺失节点。
- 权限不足：说明缺少的权限和下一步登录/授权动作，不绕过权限。
- 命令失败：保留已创建/已移动清单，告诉用户当前停在哪一步；不要继续猜测性批量操作。
